# 聊天室全螢幕優化修改

## 修改目標
將聊天室的對話框從原本只佔螢幕一半的高度，調整為佔滿整個可用螢幕空間。

## 主要修改

### 1. 移除頁面標題區域
- 移除了 `template #header` 部分
- 改在聊天室內部顯示標題

### 2. 調整容器高度
**修改前**:
```vue
<div class="flex flex-col h-96">  <!-- 固定 384px 高度 -->
```

**修改後**:
```vue
<div class="h-screen flex flex-col pt-16">  <!-- 全螢幕高度，扣除導航列 -->
```

### 3. 使用 Flexbox 佈局
- 主容器使用 `flex-1` 讓聊天區域自動擴展
- 訊息區域使用 `flex-1 min-h-0` 確保正確的高度計算
- 輸入區域使用 `flex-shrink-0` 保持固定高度

### 4. CSS 優化
- 移除了 `max-height: 400px` 限制
- 添加了 `h-screen` 確保使用 100vh
- 添加了 iOS Safari 相容性支援 (`-webkit-fill-available`)

## 佈局結構

```
AppLayout (導航列)
└── 聊天室主容器 (h-screen, pt-16)
    └── 內容容器 (flex-1)
        ├── 聊天室標題 (固定高度)
        └── 聊天容器 (flex-1)
            ├── 訊息區域 (flex-1, 可滾動)
            └── 輸入區域 (固定高度)
```

## 效果
- 🎯 聊天室現在佔滿整個可用螢幕空間
- 📱 響應式設計，適配各種螢幕尺寸  
- 🔄 訊息區域可以完全利用可用空間進行滾動
- 💻 在桌面和行動裝置上都有更好的使用體驗

## 相容性
- ✅ 桌面瀏覽器
- ✅ 行動裝置瀏覽器
- ✅ iOS Safari (特別處理了 viewport 問題)

現在聊天室將提供更沉浸式的聊天體驗！
